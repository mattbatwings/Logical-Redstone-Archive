{
  "postSchema": {
    "title": "Submission",
    "type": "object",
    "properties": {
      "description": {
        "type": "string",
        "description": "A description of the device."
      },
      "input": {
        "type": "string",
        "description": "What data does it take in (binary/hex/other)? What is the format?"
      },
      "output": {
        "type": "string",
        "description": "What data does it produce (binary/hex/other)?\nWhat is the format?"
      },
      "speed": {
        "type": "string",
        "description": "The latency (in redstone ticks) or the throughput (in hz) of the device."
      },
      "considerations": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Optional list of considerations/downsides of the device."
      },
      "notes": {
        "type": "string",
        "description": "Optional notes about the device."
      }
    },
    "required": [
      "description"
    ]
  },
  "postStyle": {
    "description": {
      "depth": 0,
      "headerText": ""
    },
    "input": {
      "depth": 2,
      "headerText": "Input"
    },
    "output": {
      "depth": 2,
      "headerText": "Output"
    },
    "speed": {
      "depth": 2,
      "headerText": "Speed"
    },
    "considerations": {
      "depth": 2,
      "headerText": "Considerations",
      "isOrdered": false
    },
    "notes": {
      "depth": 2,
      "headerText": "Notes"
    }
  }
}